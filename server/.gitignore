node_modules/
dist/
.env
*.log
*.tsbuildinfo
coverage/

/src/infrastructure/prisma/generated

# LocalStorageAdapter's default target (Task 6.2) — real proof files when running
# outside Docker; the Docker Compose stack uses a named volume instead
# (docker-compose.yml's uploads_data), never this path.
/uploads/
/test-uploads/
